

const nameLeague = (value) => {
    if(value === "ucl") {
        return (value = "Uefa Champions League")
    }
    if(value === "jl") {
        return (value = "J.League (Japon)")
    }
    if(value === "aleague") {
        return (value = "A-League Men (Australia)")
    }
    if(value === "league2") {
        return (value = "League Two (Inglaterra)")
    }
    if(value === "polcup") {
        return (value = "Puchar Polski (Polonia)")
    }
    if(value === "efl") {
        return (value = "Championship (Inglaterra)")
    }
    if (value === "bundes") {
        return (value = "Bundesliga Primera División (Alemania)");
    }
    if (value === "lig2") {
        return (value = "Liegue 2 (Francia)");
    }
    if (value === "rpl") {
        return (value = "Premier Liga (Rusa)");
    }
    if (value === "acl") {
        return (value = "AFC Champions League (Asia)");
    }
    if (value === "spfl") {
        return (value = "Premiership (Escosia)");
    }
    if (value === "dliga") {
        return (value = "SuperLiga (Dinamarca)");
    }
    if (value === "epl") {
        return (value = "Premier League (Inglaterra)");
    }
    if (value === "wsl") {
        return (value = "The FA Women's Super League (Inglaterra)");
    }
    if (value === "srb") {
        return (value = "Superliga (Serbia)");
    }
    if (value === "slig") {
        return (value = "Süper Lig (Turquía)");
    }
    if (value === "gsl") {
        return (value = "Super League (Grecia)");
    }
    if (value === "mls") {
        return (value = "Major League Soccer (EE.UU)");
    }
    if (value === "eks") {
        return (value = "League Ekstraklasa (Polonia)");
    }
    if (value === "ered") {
        return (value = "League Eredivisie (Países Bajos)");
    }
    if (value === "league1") {
        return (value = "League One (Inglaterra)");
    }
    if (value === "uel") {
        return (value = "Uefa Europa League");
    }
    if (value === "uecl") {
        return (value = "UEFA Europa Conference League");
    }
    if (value === "nos") {
        return (value = "Liga Portugal");
    }
    if (value === "football") {
        return (value = "Liga sin Identificar");
    }
    if (value === "2bundes") {
        return (value = "League 2. Bundesliga");
    }
    if (value === "ssl") {
        return (value = "Super League (Suiza) ");
    }
    if (value === "lal2") {
        return (value = "LaLiga 2 División de España");
    }
    if (value === "jupiler") {
        return (value = "League Pro League (Bélgica)");
    }
    if (value === "cdf") {
        return (value = "Coupe de France");
    }
    if (value === "cafcc") {
        return (value = "CAF Champions League (Africa)");
    }
    if (value === "seraa") {
        return (value = "League Serie A primera división (Italia)");
    }
    if (value === "uclw") {
        return (value = `UEFA Womens Champions League`);
    }
    if (value === "lig11") {
        return (value = "Ligue 1 (Francia)");
    }
    if (value === "lal") {
        return (value = "La Primera División LaLiga (España) ");
    }
    if (value === "cdllp") {
        return (value = "Copa de la Liga Profesional (Argentina)");
    }
    if (value === "nwslcc") {
        return (value = "NWSL Challenge Cup (Usa)");
    }
    if (value === "nwsl1") {
        return (value = "NWSL Challenge Cup (Usa)");
    }
    if (value === "concacafcl") {
        return (value = "CONCACAF Champions League");
    }
    if (value === "bsera") {
        return (value = "Brasileiro Série A (Brasil)");
    }
    if (value === "ligamx") {
        return (value = "Primera División de México (Liga MX)");
    }
    if (value === "lib") {
        return (value = "Copa Libertadores");
    }
    if (value === "sud") {
        return (value = "Copa Sudamericana");
    }
    if (value === "friendly") {
        return (value = "International Friendly");
    }
    if (value === "eurou21") {
        return (value = "UEFA European Under");
    }
    if (value === "eurou21") {
        return (value = "UEFA European Under");
    }
    if (value === "afcon") {
        return (value = "Africa Cup of Nations Qual.");
    }
    if (value === "undefined") {
        return (value = "Liga profesional de futbol.");
    }
};

module.exports = {
    nameLeague
}